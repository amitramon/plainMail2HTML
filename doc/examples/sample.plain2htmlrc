# -*-shell-script-*-

# Use sendmail (e.g. exim4, postfix)...
# sendmail_cmd="/usr/sbin/sendmail -oi"
# Or msmtp
sendmail_cmd=/usr/bin/msmtp

# Additional flags for plain2html
p2h_flags=-b			# encode the HTML component in 8 bit.
				# unset this variable to use base64.
